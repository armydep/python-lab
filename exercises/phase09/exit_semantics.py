"""Exercise 9.7 — predict-then-run (script, no test).

1. A context manager whose __exit__ returns True — what happens to an
   exception raised in the body?
2. A `with` body containing `return` — does cleanup still run?
3. Nested `with` blocks — what order do the cleanups run in?
Predictions first, in comments; then run; then explanations.
"""

# TODO
