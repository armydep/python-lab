"""Exercise 12.1 — pdb practice (script, no test).

1. Write summarize_scores(rows) with THREE planted bugs: an off-by-one
   slice, a mutable default argument, a wrong dict key. (Plant them
   honestly, then take a day's break if you can — fresh eyes.)
2. Find all three USING ONLY pdb: python -m pdb debug_me.py, or insert
   breakpoint(). No prints, no staring.
3. Record the pdb commands you used (n/s/c/l/p/pp/w/up) in comments.

Skills practiced:
- Debugging with pdb (n/s/c/p/w)
- Finding planted bugs without print statements
"""

# TODO
